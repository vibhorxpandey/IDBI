# --- Python ---
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# --- Models & generated artefacts (reproducible via `make all`) ---
*.pkl
data/synthetic/*.csv
data/processed/*
!data/processed/.gitkeep
!data/processed/leads.json
!data/processed/uplift_curve.json
!data/processed/fairness_summary.json


# docs/uplift_curve.png is intentionally TRACKED — it's the README's hero image

# --- Optional raw dataset (large, user-supplied) ---
data/raw/*.csv

# --- Node / dashboard ---
node_modules/
dashboard/dist/
dashboard/.vite/

# --- Editors / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db
.env

# --- Local scratch / bundles (not for the repo) ---
*.zip
gitignore-patch.txt
.vercel
