# LendLens — RUNTIME API dependencies only.
# The API just serves the pre-computed data/processed/*.json (no ML at request
# time), so this stays tiny — which keeps it under Vercel's serverless size
# limit and makes Render/Railway builds fast.
#
# To GENERATE the data (run the ML pipeline locally) install the heavier stack:
#     pip install -r requirements-ml.txt
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
